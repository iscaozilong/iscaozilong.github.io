---
title: 每日日报｜2026-09-18
date: 2026-09-18 03:00:00
tags:
  - 日报
  - 自动化
categories: 工作记录
description: 自动汇总过去一天的工作记录（已过滤敏感信息）。
---

## 今日完成

- 完成 Hermes Agent 运行时迁移与升级，统一启动器、网关服务和 MCP 子进程使用 v0.21.3；清理确认无引用的旧运行时副本及轮转日志，回收约 4 GB 空间。
- 修复 Cloudflare 账户级凭据的资源范围问题，补齐站点级访问能力；配置终端自检脚本和官方 Wrangler 工具，保留回滚备份。
- 修复 sherpa 唤醒词引擎缺少 `pypinyin` 的依赖问题。新增仅声明依赖的 Hermes 插件，使依赖可在后续更新中重新安装。
- 完成工作目录整理，将确认无引用的历史备份、一次性报告和临时产物集中归档，并完成回归检查。

## 问题与解决

- Hermes 旧运行目录名称与实际版本不一致，且运行时目录曾处于拆分状态。通过复制到正式版本目录、修正内置路径并更新启动入口，消除旧目录引用。
- Cloudflare 账户级凭据在用户级接口上会出现误导性的无效提示，实际问题是缺少站点资源范围。改为按账户和站点两个层面验证并补齐权限。
- `sherpa-onnx` 的依赖元数据未包含 `pypinyin`，导致首次构建失败。通过依赖插件持久化修复，未直接修改运行时源码。

## 验证结果

- Hermes v0.21.3 CLI 可正常运行，网关状态为 active，启动器、网关和 MCP 进程指向同一运行时；运行时工作区干净。
- Cloudflare 自检中账户、站点、DNS、设置、规则集、Workers 和 Access 相关读操作均返回成功；MCP 连接测试成功并发现 3 个工具。
- 唤醒词插件在真实插件管理器中启用且无错误；`pypinyin` 已安装，离线正向音频触发 1 次，负向对照触发 0 次。
- 目录整理后的回归检查通过，未发现活跃服务依赖被移动内容。

## 待跟进

- 需要在真实麦克风输入下再验收一次唤醒词链路；当前仅完成离线音频验证。
- Hermes 当前发行版已是 v0.21.3，但上游 `main` 仍有尚未进入发行版的提交，暂不把两者写成完全同步。
- 构建期依赖安全告警仍等待上游 lockfile 更新；Cloudflare 的 R2 功能仍需在控制台启用后再复测。
- 其他隔离配置的凭据同步与旧凭据回收需在确认依赖后处理。
